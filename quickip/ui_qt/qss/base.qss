/* ═══════════════════════════════════════════════════════════════
   NetConneXion v2.0 — LIGHT THEME (F: White + Indigo)
   Backdrop: {LIGHT_PANEL_BG}  Sidebar: {LIGHT_CARD_SURFACE}  Cards: {LIGHT_CARD_SURFACE}
   Accent: {LIGHT_ACCENT} indigo
   ═══════════════════════════════════════════════════════════════ */

* {
    font-family: "Segoe UI", system-ui, sans-serif;
    font-size: 14px;
    outline: none;
}

QWidget {
    color: {LIGHT_TEXT_PRIMARY};           /* глобальный текст — slate-800 */
    background: transparent;
}

QWidget#RootWindow {
    background: transparent;  /* фон рисует BackdropWidget ({LIGHT_PANEL_BG}) */
}

/* ── SIDEBAR ──────────────────────────────────────────────────── */
QFrame#Sidebar {
    background: {LIGHT_CARD_SURFACE};      /* sidebar — белый */
    border: 1px solid {LIGHT_DATA_SURFACE}; /* рамка sidebar */
    border-radius: 14px;
}

/* Logo area */
QFrame#LogoArea {
    background: transparent;
    border-bottom: 1px solid {LIGHT_SIDE_SURFACE}; /* разделитель под логотипом */
    border-radius: 0;
}

QLabel#LogoIcon {
    background: {LIGHT_ACCENT};      /* иконка-плашка N — indigo */
    color: {LIGHT_TEXT_ON_ACCENT};
    font-size: 13px;
    font-weight: 700;
    border-radius: 8px;
}

QLabel#LogoName {
    font-size: 13px;
    font-weight: 700;
    color: {LIGHT_TEXT_PRIMARY};           /* название приложения */
}

QLabel#LogoVersion {
    font-size: 9px;
    color: {LIGHT_TEXT_MUTED};           /* версия — slate-400 */
}

/* Nav */
QFrame#NavFrame {
    background: transparent;
}

QLabel#NavGroupLabel {
    font-size: 10px;
    font-weight: 700;
    color: {LIGHT_BORDER_SUBTLE};           /* заголовок группы: NETWORK / SYSTEM */
    letter-spacing: 2px;
    text-transform: uppercase;
    padding: 0 8px 4px 8px;
}

QPushButton#NavBtn {
    text-align: left;
    padding: 0 12px 0 14px;
    min-height: 44px;
    border-radius: 8px;
    background: transparent;
    border: none;
    border-left: 3px solid transparent;
    color: {LIGHT_TEXT_FAINT};           /* неактивная nav кнопка — slate-500 */
    font-size: 15px;
    font-weight: 500;
}

QPushButton#NavBtn:hover {
    background: {LIGHT_PANEL_BG};
    color: {LIGHT_TEXT_PRIMARY};
    border-left: 3px solid {LIGHT_BORDER_SUBTLE};
}

QPushButton#NavBtn[active="true"] {
    background: {LIGHT_INPUT_BG};      /* активная nav — indigo-50 */
    color: {LIGHT_ACCENT_PRESSED};           /* текст активной — indigo-700 */
    font-weight: 700;
    border-left: 3px solid {LIGHT_ACCENT_PRESSED};
}

/* Status block */
QFrame#StatusBlock {
    background: transparent;
    border-top: 1px solid {LIGHT_SIDE_SURFACE}; /* разделитель над статусом */
    border-radius: 0;
}

QLabel#StatusDot {
    color: {LIGHT_STATUS_SUCCESS};           /* зелёная точка — connected */
    font-size: 8px;
}

QLabel#StatusActive {
    font-size: 10px;
    font-weight: 600;
    color: {LIGHT_TEXT_STRONG};           /* активный профиль */
}

QLabel#StatusKey {
    font-size: 10px;
    color: {LIGHT_TEXT_MUTED};           /* ключ: "Adapter" */
}

QLabel#StatusValue {
    font-size: 10px;
    font-weight: 600;
    color: {LIGHT_TEXT_STRONG};           /* значение: "Ethernet" */
}

QLabel#StatusIP {
    font-size: 10px;
    color: {LIGHT_ACCENT};           /* IP адрес — indigo */
    font-family: "Consolas", "Courier New", monospace;
    font-weight: 600;
}

/* ── TOPBAR ───────────────────────────────────────────────────── */
QFrame#Topbar {
    background: {LIGHT_CARD_SURFACE};      /* topbar — белый */
    border: 1px solid {LIGHT_DATA_SURFACE};
    border-radius: 12px;
}

QLabel#TopbarTitle {
    font-size: 15px;
    font-weight: 700;
    color: {LIGHT_TEXT_PRIMARY};           /* заголовок страницы */
}

QLabel#TopbarSubtitle {
    font-size: 10px;
    color: {LIGHT_TEXT_MUTED};           /* подзаголовок: "4 saved · Office active" */
}

/* ── CONTENT SHELL ────────────────────────────────────────────── */
QFrame#ContentShell {
    background: {LIGHT_CARD_SURFACE};      /* основная рабочая область — белый */
    border: 1px solid {LIGHT_DATA_SURFACE};
    border-radius: 14px;      /* скругление всех углов */
}

/* ── LEFT PANEL ───────────────────────────────────────────────── */
QFrame#LeftPanel {
    background: {LIGHT_CARD_SURFACE};      /* левая панель — белый */
    border-right: 1px solid {LIGHT_DATA_SURFACE}; /* разделитель между панелями */
    border-radius: 14px 0 14px 14px; /* все углы кроме верхнего правого */
}

QFrame#PanelHeader {
    background: transparent;
    border-bottom: 1px solid {LIGHT_PANEL_BG}; /* разделитель под заголовком */
}

QLabel#PanelTitle {
    font-size: 10px;
    font-weight: 700;
    color: {LIGHT_BORDER_SUBTLE};           /* заголовок "All profiles" */
    text-transform: uppercase;
    letter-spacing: 1.2px;
}

QLabel#CountPill {
    background: {LIGHT_INPUT_BG};      /* пилюля счётчика — indigo-50 */
    color: {LIGHT_ACCENT_DEEP};           /* текст счётчика — indigo-800 */
    font-size: 9px;
    font-weight: 700;
    padding: 2px 7px;
    border-radius: 99px;
}

QFrame#SearchWrap {
    background: transparent;
}

QLineEdit#ProfilesSearch {
    background: {LIGHT_APP_BG};      /* фон поля поиска */
    border: 1px solid {LIGHT_BORDER_DEFAULT}; /* рамка поиска */
    border-radius: 8px;
    padding: 0 10px;
    min-height: 32px;
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 12px;
}

QLineEdit#ProfilesSearch:focus {
    border-color: {LIGHT_ACCENT_TEXT};   /* рамка поиска при фокусе — indigo-300 */
    background: {LIGHT_CARD_SURFACE};
}

QComboBox#AdapterFilter {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    padding: 0 10px;
    min-height: 32px;
    color: {LIGHT_TEXT_FAINT};
    font-size: 12px;
}

QComboBox#AdapterFilter:focus,
QComboBox#AdapterFilter:on {
    border-color: {LIGHT_ACCENT_TEXT};
    background: {LIGHT_CARD_SURFACE};
}

QComboBox#AdapterFilter::drop-down {
    border: none;
    width: 28px;
    background: transparent;
}

QComboBox#AdapterFilter::down-arrow {
    image: none;
    width: 8px;
    height: 8px;
    border-left: 2px solid {LIGHT_TEXT_MUTED};
    border-bottom: 2px solid {LIGHT_TEXT_MUTED};
    margin-top: -4px;
}

/* Profile list */
QListWidget#ProfilesList {
    background: transparent;
    border: none;
    outline: none;
    padding: 4px 0 0 0;   /* нижний padding убран — убирает пустоту после последней строки */
}

QListWidget#ProfilesList::viewport {
    background: transparent;
}

QListWidget#ProfilesList::item {
    background: transparent;
    border: none;
    padding: 0;
}

QListWidget#ProfilesList::item:selected {
    background: transparent; /* выделение рисуется в ProfileRowWidget.paintEvent */
}

QWidget#ProfileRow {
    background: transparent;
}

QWidget#ProfileRow:hover {
    background: {LIGHT_APP_BG};      /* hover строки профиля — очень лёгкий */
    border-radius: 10px;
}

QLabel#ProfileName {
    font-size: 14px;
    font-weight: 600;
    color: {LIGHT_TEXT_PRIMARY};           /* имя профиля */
    letter-spacing: -0.1px;
}

/* Адаптер + режим в одну строку — "Ethernet · STATIC" */
QLabel#ProfileMetaStatic {
    font-size: 12px;
    color: {LIGHT_TEXT_MUTED};           /* базовый цвет строки адаптера */
}

QLabel#ProfileMetaDhcp {
    font-size: 12px;
    color: {LIGHT_TEXT_MUTED};           /* то же, DHCP */
}

/* Actions block */
QFrame#ActionsBlock {
    background: {LIGHT_CARD_SURFACE};      /* фон блока кнопок — белый, единый с панелью */
    border-top: 1px solid {LIGHT_PANEL_BG};
    border-radius: 0 0 14px 14px; /* скругление нижних углов — совпадает с LeftPanel */
}

/* ── RIGHT PANEL ──────────────────────────────────────────────── */
QFrame#RightPanel {
    background: {LIGHT_APP_BG};      /* фон правой панели — чуть светлее белого */
    border-radius: 0 14px 14px 0;  /* скругление только правых углов */
}

QFrame#EditorHeader {
    background: {LIGHT_CARD_SURFACE};      /* хедер редактора — белый, без внешней рамки */
    border-bottom: 1px solid {LIGHT_PANEL_BG}; /* тонкий разделитель снизу */
    border-top-right-radius: 13px;    /* скругление верхнего правого угла под ContentShell */
    border-top-left-radius: 0;
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
}

QWidget#EditorBody {
    background: transparent;
}

QScrollArea#EditorScroll {
    background: transparent;
    border: none;
}

QScrollArea#EditorScroll > QWidget > QWidget {
    background: transparent;
}

/* Section cards */
QFrame#SectionCard {
    background: {LIGHT_CARD_SURFACE};      /* фон секции General / Network / DNS */
    border: 1px solid {LIGHT_DATA_SURFACE}; /* рамка секции */
    border-radius: 12px;
}

QLabel#SectionTitle {
    font-size: 11px;
    font-weight: 700;
    color: {LIGHT_BORDER_SUBTLE};           /* заголовок секции — GENERAL, NETWORK, DNS */
    letter-spacing: 1.5px;
}

QFrame#SectionLine {
    color: {LIGHT_PANEL_BG};           /* горизонтальная линия после заголовка секции */
}

QWidget#SettingsRow {
    background: transparent;
}

QLabel#SettingsHint {
    font-size: 11px;
    color: {LIGHT_TEXT_MUTED};
}

QLabel#FieldLabel {
    font-size: 14px;
    color: {LIGHT_TEXT_FAINT};           /* подпись поля: Name, IP address и т.д. */
    font-weight: 500;
}

/* ── INPUTS ───────────────────────────────────────────────────── */
QLineEdit, QComboBox {
    background: {LIGHT_APP_BG};      /* фон полей ввода */
    border: 1px solid {LIGHT_BORDER_DEFAULT}; /* рамка полей */
    border-radius: 8px;
    padding: 0 12px;
    min-height: 38px;
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 14px;
    font-family: "Consolas", "Courier New", monospace; /* monospace для IP-адресов */
}

QLineEdit:focus, QComboBox:focus {
    border-color: {LIGHT_ACCENT_TEXT};   /* рамка при фокусе — indigo-300 */
    background: {LIGHT_CARD_SURFACE};
}

QComboBox:on {
    border-color: {LIGHT_ACCENT_TEXT};
    background: {LIGHT_CARD_SURFACE};
}

QLineEdit:disabled, QComboBox:disabled {
    background: {LIGHT_PANEL_BG};      /* заблокированное поле */
    color: {LIGHT_BORDER_SUBTLE};
    border-color: {LIGHT_BORDER_DEFAULT};
}

QComboBox::drop-down {
    width: 26px;
    border-left: 1px solid {LIGHT_BORDER_DEFAULT};
    background: transparent;
    border-radius: 0 7px 7px 0;
}

QComboBox::down-arrow {
    image: none;
    border-left: 2px solid {LIGHT_TEXT_MUTED};  /* стрелка ComboBox */
    border-bottom: 2px solid {LIGHT_TEXT_MUTED};
    width: 6px;
    height: 6px;
    margin-top: -2px;
}

QComboBox QAbstractItemView {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    selection-background-color: {LIGHT_INPUT_BG}; /* выбранный элемент */
    selection-color: {LIGHT_TEXT_PRIMARY};
    padding: 4px;
    color: {LIGHT_TEXT_PRIMARY};
}

/* ── BUTTONS ──────────────────────────────────────────────────── */
QPushButton {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 7px;
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
    font-weight: 600;
    padding: 6px 12px;
    /* min-height: 30px — ГЛОБАЛЬНЫЙ минимум для всех QPushButton.
       Перебивается только через min-height: 0 в более специфичном селекторе.
       Для BtnSave и BtnApply снято ниже в QPushButton#BtnSave/BtnApply.
       Размер этих кнопок управляется через setFixedSize() в main_window.py */
    min-height: 30px;
}

/* ── BtnSave (кнопка Save в EditorHeader) ─────────────────────────────────
   Размер задаётся через setFixedSize(ширина, высота) в main_window.py.
   min-height: 0 и max-height: 9999px — снимают ограничение глобального
   QPushButton { min-height: 30px } выше, иначе высота не опустится < 30px.
   Шрифт задаётся здесь — не в Python, не в role="action". */
QPushButton#BtnSave {
    font-size: 12px;
    font-weight: 600;
    min-height: 0;       /* снимает глобальный min-height: 30px */
    max-height: 9999px;  /* снимает возможные max-height от других правил */
}

/* ── BtnApply (кнопка Apply Profile в EditorHeader) ──────────────────────
   То же самое что BtnSave. Размер — setFixedSize() в main_window.py. */
QPushButton#BtnApply {
    font-size: 12px;
    font-weight: 700;
    min-height: 0;
    max-height: 9999px;
}

QPushButton:hover {
    background: {LIGHT_PROFILE_CARD_BG};
    border-color: {LIGHT_CARD_BORDER};
    color: {LIGHT_TEXT_PRIMARY};
}

QPushButton:pressed {
    background: {LIGHT_STATE_ACTIVE_BG};
}

QPushButton[role="action"] {
    background: {LIGHT_APP_BG};      /* action кнопки — Save, Копировать, Импорт, Экспорт */
    border-color: {LIGHT_BORDER_DEFAULT};
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
}

QPushButton[role="action"]:hover {
    background: {LIGHT_PROFILE_CARD_BG};
    border-color: {LIGHT_CARD_BORDER};
    color: {LIGHT_TEXT_PRIMARY};
}

QPushButton[role="primary"] {
    background: {LIGHT_WIFI_TABLE_BG};
    border: 1px solid {LIGHT_CONTROL_BORDER};
    color: {LIGHT_ACCENT_DEEP};
    font-weight: 700;
    font-size: 12px;
}

QPushButton[role="primary"]:hover {
    background: {LIGHT_STATE_HOVER_BG};
    border-color: {LIGHT_SCROLL_HANDLE};
}

QPushButton[role="primary"]:pressed {
    background: {LIGHT_CONTROL_BORDER};
}

QPushButton[role="delete"] {
    background: {LIGHT_ERROR_BG};      /* Delete — слабо-красный фон в покое */
    border: 1px solid {LIGHT_ERROR_BORDER};
    color: {LIGHT_STATUS_ERROR};
    font-weight: 600;
    font-size: 12px;
}

QPushButton[role="delete"]:hover {
    background: {LIGHT_ERROR_BG_HOVER};
    border-color: {LIGHT_ERROR_TEXT};
    color: {LIGHT_STATUS_ERROR_STRONG};
}

QPushButton[role="delete"]:pressed {
    background: {LIGHT_ERROR_BORDER};
}

QPushButton:disabled {
    background: {LIGHT_PANEL_BG};
    border-color: {LIGHT_BORDER_DEFAULT};
    color: {LIGHT_TEXT_MUTED};
}

/* ── SCROLLBARS ───────────────────────────────────────────────── */
QScrollBar:vertical {
    width: 5px;
    background: transparent;
    margin: 4px 1px;
}

QScrollBar::handle:vertical {
    background: {LIGHT_RGBA_15_23_42_0_12}; /* ползунок */
    border-radius: 3px;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background: {LIGHT_RGBA_15_23_42_0_22};
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    height: 0;
    background: transparent;
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: transparent;
}

/* ── PLACEHOLDER ──────────────────────────────────────────────── */
QLabel#Placeholder {
    font-size: 18px;
    font-weight: 300;
    color: {LIGHT_BORDER_SUBTLE};           /* текст-заглушка на пустых страницах */
}

/* ── DIALOGS (QMessageBox, QDialog, QFileDialog) ──────────────── */
QDialog {
    background: {LIGHT_CARD_SURFACE};
    color: {LIGHT_TEXT_PRIMARY};
}

QDialog QLabel {
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 13px;
    background: transparent;
}

QMessageBox {
    background: {LIGHT_CARD_SURFACE};
    color: {LIGHT_TEXT_PRIMARY};
}

QMessageBox QLabel {
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 13px;
    background: transparent;
}

QMessageBox QPushButton {
    min-width: 72px;
    min-height: 32px;
    font-size: 13px;
}

/* ── WI-FI PAGE ───────────────────────────────────────────────── */
QTableWidget#WifiNetworkTable,
QTableWidget#WifiProfileTable {
    background: transparent;
    border: none;
    outline: none;
    gridline-color: transparent;
    selection-background-color: transparent;
}

QTableWidget#WifiNetworkTable::item,
QTableWidget#WifiProfileTable::item {
    padding: 4px 8px;
    border: none;
    background: transparent;
    color: {LIGHT_TEXT_STRONG};
}

QTableWidget#WifiNetworkTable::item:selected,
QTableWidget#WifiProfileTable::item:selected {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_TEXT_PRIMARY};
    border-radius: 6px;
}

QHeaderView::section {
    background: transparent;
    color: {LIGHT_TEXT_MUTED};
    font-size: 11px;
    font-weight: 600;
    padding: 4px 8px;
    border: none;
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
}

QFrame#WifiStatusBar {
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
    background: transparent;
}

QLabel#WifiStatusLabel {
    font-size: 13px;
    color: {LIGHT_TEXT_SECONDARY};
}

QLabel#WifiFeedback {
    font-size: 13px;
    padding: 4px 0;
}

QCheckBox {
    font-size: 13px;
    color: {LIGHT_TEXT_SECONDARY};
    spacing: 6px;
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border-radius: 4px;
    border: 1px solid {LIGHT_BORDER_SUBTLE};
    background: {LIGHT_APP_BG};
}

QCheckBox::indicator:checked {
    background: {LIGHT_ACCENT_HOVER};
    border-color: {LIGHT_ACCENT_HOVER};
}

QFrame#WifiTopPanel {
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
}

QFrame#WifiBottomPanel {
    background: transparent;
}

/* ── WI-FI TABS ───────────────────────────────────────────────── */
QTabWidget#WifiTabs {
    border: none;
}

QTabWidget#WifiTabs::pane {
    border: none;
    background: transparent;
}

QTabWidget#WifiTabs > QTabBar::tab {
    background: transparent;
    color: {LIGHT_TEXT_MUTED};
    font-size: 13px;
    font-weight: 500;
    padding: 10px 20px;
    border: none;
    border-bottom: 2px solid transparent;
    min-width: 100px;
}

QTabWidget#WifiTabs > QTabBar::tab:selected {
    color: {LIGHT_ACCENT_HOVER};
    border-bottom: 2px solid {LIGHT_ACCENT_HOVER};
}

QTabWidget#WifiTabs > QTabBar::tab:hover:!selected {
    color: {LIGHT_TEXT_SECONDARY};
    border-bottom: 2px solid {LIGHT_BORDER_SUBTLE};
}

QFrame#WifiProfileToolbar {
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
    background: transparent;
}

QFrame#WifiProfileToolbar QPushButton {
    min-height: 0;
    max-height: 9999px;
}

QLabel#WifiStatusDetails {
    font-size: 11px;
    color: {LIGHT_TEXT_FAINT};
    padding-left: 22px;
    background: transparent;
}

QMenu {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    padding: 4px;
}
QMenu::item {
    padding: 6px 16px;
    border-radius: 4px;
    font-size: 13px;
    color: {LIGHT_TEXT_PRIMARY};
}
QMenu::item:selected { background: {LIGHT_INPUT_BG}; color: {LIGHT_ACCENT}; }
QMenu::separator { height: 1px; background: {LIGHT_BORDER_DEFAULT}; margin: 3px 8px; }

/* ── TOOLS PAGE ───────────────────────────────────────────────── */
QFrame#ToolsSidebar {
    border-right: 1px solid {LIGHT_BORDER_DEFAULT};
    background: transparent;
}

/* ── Tool nav items ── */
QFrame#ToolNavItem {
    background: transparent;
    border-radius: 0;
}
QFrame#ToolNavItem:hover {
    background: {LIGHT_PANEL_BG};
}
QFrame#ToolNavItem[active="true"] {
    background: {LIGHT_INPUT_BG};
}

QFrame#ToolNavBar {
    background: transparent;
    border-radius: 0 2px 2px 0;
}
QFrame#ToolNavBar[active="true"] {
    background: {LIGHT_ACCENT};
}

QLabel#ToolNavIcon {
    color: {LIGHT_TEXT_MUTED};
    font-size: 13px;
}
QLabel#ToolNavIcon[active="true"] {
    color: {LIGHT_ACCENT};
}

QLabel#ToolNavText {
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
    padding-right: 4px;
}
QLabel#ToolNavText[active="true"] {
    color: {LIGHT_ACCENT};
    font-weight: 600;
}

QLabel#ToolGroupLabel {
    font-size: 13px;
    font-weight: 700;
    color: {LIGHT_TEXT_MUTED};
    padding-left: 14px;
    padding-top: 2px;
    padding-bottom: 2px;
}

QFrame#ToolDivider {
    background: {LIGHT_BORDER_DEFAULT};
    max-height: 1px;
    margin: 0 10px;
}

QLabel#ToolPanelTitle {
    font-size: 15px;
    font-weight: 500;
    color: {LIGHT_TEXT_PRIMARY};
}

/* Subnet Calculator */
QLabel#SubnetLabel {
    font-size: 12px;
    color: {LIGHT_TEXT_FAINT};
    min-width: 140px;
}
QLabel#SubnetValue {
    font-size: 12px;
    font-weight: 600;
    color: {LIGHT_TEXT_PRIMARY};
}

/* Speed Test cards */
QFrame#SpeedCard {
    background: {LIGHT_PANEL_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 10px;
}
QLabel#SpeedCardTitle {
    font-size: 11px;
    color: {LIGHT_TEXT_FAINT};
}
QLabel#SpeedCardValue {
    font-size: 24px;
    font-weight: 700;
    color: {LIGHT_TEXT_PRIMARY};
}
QLabel#SpeedCardUnit {
    font-size: 11px;
    color: {LIGHT_TEXT_MUTED};
}

QTextEdit#ToolOutput {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    color: {LIGHT_TEXT_PRIMARY};
    selection-background-color: {LIGHT_ACCENT_HOVER};
    selection-color: {LIGHT_TEXT_ON_ACCENT};
    padding: 6px;
    margin-top: 10px;
}

/* Netstat / Routes / DNS Cache shared tree */
QTreeWidget#NetstatTable,
QTreeWidget#ToolTree,
QTreeWidget#IpconfigTree {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    outline: none;
}
QTreeWidget#NetstatTable::item,
QTreeWidget#ToolTree::item,
QTreeWidget#IpconfigTree::item {
    padding: 3px 6px;
    color: {LIGHT_TEXT_PRIMARY};
}
QTreeWidget#NetstatTable::item:hover,
QTreeWidget#ToolTree::item:hover,
QTreeWidget#IpconfigTree::item:hover {
    background: {LIGHT_PANEL_BG};
    color: {LIGHT_TEXT_PRIMARY};
}
QTreeWidget#NetstatTable::item:selected,
QTreeWidget#NetstatTable::item:selected:active,
QTreeWidget#NetstatTable::item:selected:!active,
QTreeWidget#ToolTree::item:selected,
QTreeWidget#ToolTree::item:selected:active,
QTreeWidget#ToolTree::item:selected:!active,
QTreeWidget#IpconfigTree::item:selected,
QTreeWidget#IpconfigTree::item:selected:active,
QTreeWidget#IpconfigTree::item:selected:!active {
    background: {LIGHT_ACCENT_HOVER};
    color: {LIGHT_TEXT_ON_ACCENT};
}
QTreeWidget#NetstatTable QHeaderView::section,
QTreeWidget#ToolTree QHeaderView::section,
QTreeWidget#IpconfigTree QHeaderView::section {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_ACCENT};
    font-size: 11px;
    font-weight: 700;
    padding: 4px 6px;
    border: none;
    border-bottom: 1px solid {LIGHT_CARD_BORDER};
}

QTableWidget#NetstatTable {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    outline: none;
    gridline-color: {LIGHT_BORDER_DEFAULT};
}
QTableWidget#NetstatTable::item {
    padding: 3px 6px;
    color: {LIGHT_TEXT_PRIMARY};
    border: none;
}
QTableWidget#NetstatTable::item:hover {
    background: {LIGHT_PANEL_BG};
}
QTableWidget#NetstatTable::item:selected,
QTableWidget#NetstatTable::item:selected:active,
QTableWidget#NetstatTable::item:selected:!active {
    background: {LIGHT_ACCENT_HOVER};
    color: {LIGHT_TEXT_ON_ACCENT};
}
QTableWidget#NetstatTable QHeaderView::section {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_ACCENT};
    font-size: 11px;
    font-weight: 700;
    padding: 4px 6px;
    border: none;
    border-bottom: 1px solid {LIGHT_CARD_BORDER};
    border-right: 1px solid {LIGHT_CARD_BORDER};
}
QTableWidget#NetstatTable QHeaderView::section:last {
    border-right: none;
}

QTreeWidget#IpconfigTree {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 8px;
    alternate-background-color: {LIGHT_PANEL_BG};
    margin-top: 10px;
    outline: none;
}
QTreeWidget#IpconfigTree::item {
    padding: 3px 6px;
    border: none;
    color: {LIGHT_TEXT_PRIMARY};
}
QTreeWidget#IpconfigTree::branch {
    background: transparent;
    border: none;
}
QTreeWidget#IpconfigTree::branch:hover,
QTreeWidget#IpconfigTree::branch:selected,
QTreeWidget#IpconfigTree::branch:selected:active,
QTreeWidget#IpconfigTree::branch:hover:selected {
    background: transparent;
    border: none;
}
QTreeWidget#IpconfigTree::branch:has-children:!has-siblings:closed,
QTreeWidget#IpconfigTree::branch:has-children:has-siblings:closed {
    image: url({ASSETS}/arrow-right.svg);
    background: transparent;
}
QTreeWidget#IpconfigTree::branch:has-children:!has-siblings:open,
QTreeWidget#IpconfigTree::branch:has-children:has-siblings:open {
    image: url({ASSETS}/arrow-down.svg);
    background: transparent;
}
QTreeWidget#IpconfigTree QHeaderView::section {
    background: {LIGHT_INPUT_BG};
    border: none;
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
    border-right: 1px solid {LIGHT_BORDER_DEFAULT};
    padding: 4px 8px;
    font-weight: 600;
    color: {LIGHT_TEXT_SECONDARY};
}

QLabel#ToolStatus {
    font-size: 12px;
    color: {LIGHT_TEXT_FAINT};
}

QLineEdit#ToolInput {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 6px;
    color: {LIGHT_TEXT_PRIMARY};
    padding: 0 8px;
    font-size: 13px;
}

QLineEdit#ToolInput:focus {
    border-color: {LIGHT_ACCENT_HOVER};
    background: {LIGHT_PAGE_CANVAS};
}

QSpinBox#ToolSpinBox {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 6px;
    color: {LIGHT_TEXT_PRIMARY};
    padding: 0 4px;
    font-size: 13px;
}

/* ── TOOLS BUTTONS ANIMATION ──────────────────────────────────── */
QPushButton#ToolBtn {
}

QPushButton#ToolBtn:pressed {
    padding-top: 2px;
    padding-bottom: -2px;
    opacity: 0.85;
}

QPushButton#ToolBtn[role="primary"]:pressed {
    background: {LIGHT_ACCENT_PRESSED};
    border-color: {LIGHT_ACCENT_DEEP};
}

QPushButton#ToolBtn[role="action"]:pressed {
    background: {LIGHT_BORDER_SUBTLE};
}

QPushButton#ToolNavBtn:pressed {
    padding-top: 2px;
    background: {LIGHT_SECTION_DIVIDER};
}

/* DNS type combo */
QComboBox#ToolCombo {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 6px;
    color: {LIGHT_TEXT_PRIMARY};
    padding: 0 8px;
    font-size: 13px;
    min-height: 28px;
    max-width: 180px;
}

QComboBox#ToolCombo::drop-down {
    border: none;
    width: 20px;
}

QComboBox#ToolCombo:focus {
    border-color: {LIGHT_ACCENT_HOVER};
}

QSpinBox#ToolSpinBox:disabled {
    color: {LIGHT_TEXT_MUTED};
    background: {LIGHT_PANEL_BG};
}

QSpinBox#SettingsSpinBox {
    background: {LIGHT_APP_BG};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 7px;
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 13px;
    padding: 0 8px;
    min-height: 0;
    max-height: 9999px;
}

QSpinBox#SettingsSpinBox::up-button,
QSpinBox#SettingsSpinBox::down-button {
    width: 0;
    border: none;
}
/* Subnet Calculator / Калькулятор подсетей */
QFrame#SubnetInputCard,
QFrame#SubnetResultCard {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 10px;
}
QLabel#SubnetInputLabel {
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
    font-weight: 600;
}
QLabel#SubnetResultsTitle {
    color: {LIGHT_TEXT_PRIMARY};
    font-size: 13px;
    font-weight: 700;
}
QLabel#SubnetCardTitle {
    color: {LIGHT_ACCENT};
    font-size: 11px;
    font-weight: 700;
    padding-bottom: 5px;
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
}
QFrame#SubnetResultCard QLabel#SubnetLabel {
    min-width: 150px;
}
/* TCP Port Scanner / Сканер TCP-портов */
QFrame#PortScanInputCard,
QFrame#PortScanResultCard {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 10px;
}
QLabel#PortScanInputLabel {
    background: {LIGHT_CARD_SURFACE};
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
    font-weight: 600;
}
QLabel#PortScanProgressText {
    color: {LIGHT_TEXT_FAINT};
    font-size: 12px;
}
QProgressBar#PortScanProgress {
    background: {LIGHT_BORDER_DEFAULT};
    border: none;
    border-radius: 3px;
}
QProgressBar#PortScanProgress::chunk {
    background: {LIGHT_ACCENT_HOVER};
    border-radius: 3px;
}
QTableWidget#PortScanTable {
    background: {LIGHT_CARD_SURFACE};
    alternate-background-color: {LIGHT_APP_BG};
    color: {LIGHT_TEXT_PRIMARY};
    border: none;
    border-radius: 10px;
}
QTableWidget#PortScanTable::item {
    padding: 7px 10px;
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
}
QTableWidget#PortScanTable::item:selected {
    background: {LIGHT_STATE_ACTIVE_BG};
    color: {LIGHT_TEXT_PRIMARY};
}
QTableWidget#PortScanTable QHeaderView::section {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_ACCENT};
    border: none;
    border-bottom: 1px solid {LIGHT_CARD_BORDER};
    padding: 8px 10px;
    font-weight: 700;
}
/* Windows DNS Cache / Кэш DNS Windows */
QFrame#DnsCacheToolbarCard,
QFrame#DnsCacheResultCard {
    background: {LIGHT_CARD_SURFACE};
    border: 1px solid {LIGHT_BORDER_DEFAULT};
    border-radius: 10px;
}
QLabel#DnsCacheToolbarLabel {
    background: {LIGHT_CARD_SURFACE};
    color: {LIGHT_TEXT_SECONDARY};
    font-size: 12px;
    font-weight: 600;
}
QWidget#DnsCacheControlsBar {
    background: transparent;
    border: none;
}
QLabel#DnsCacheSummaryPill {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_ACCENT};
    border: 1px solid {LIGHT_CARD_BORDER};
    border-radius: 10px;
    padding: 3px 10px;
    font-size: 11px;
    font-weight: 600;
}
QTreeWidget#DnsCacheTable {
    background: {LIGHT_CARD_SURFACE};
    alternate-background-color: {LIGHT_APP_BG};
    color: {LIGHT_TEXT_PRIMARY};
    border: none;
    border-radius: 10px;
}
QTreeWidget#DnsCacheTable::item {
    padding: 7px 10px;
    border-bottom: 1px solid {LIGHT_BORDER_DEFAULT};
}
QTreeWidget#DnsCacheTable QHeaderView::section {
    background: {LIGHT_INPUT_BG};
    color: {LIGHT_ACCENT};
    border: none;
    border-bottom: 1px solid {LIGHT_CARD_BORDER};
    padding: 8px 10px;
    font-weight: 700;
}


/* IP Batch Check structured workspace */
QFrame#IpBatchSourceCard {
    background: #ffffff;
    border: 1px solid #d8e1ee;
    border-radius: 12px;
}
QLabel#IpBatchFieldLabel {
    color: #526987;
    font-size: 12px;
    font-weight: 600;
}
QLabel#IpBatchFileLabel {
    color: #637a99;
    padding: 0 10px;
}
QLabel#IpBatchSummaryPill {
    color: #425a78;
    background: #e8eef9;
    border-radius: 11px;
    padding: 4px 10px;
    font-size: 11px;
    font-weight: 600;
}
QFrame#IpBatchProgressRow {
    background: #f7f9fc;
    border: 1px solid #d8e1ee;
    border-radius: 9px;
}
QFrame#IpBatchProgressRow QLabel#ToolStatus {
    color: #637a99;
    background: transparent;
    border: none;
    font-size: 11px;
}
QProgressBar#IpBatchProgress {
    background: #e1e8f2;
    border: none;
    border-radius: 3px;
    color: transparent;
}
QProgressBar#IpBatchProgress::chunk {
    background: #6674f7;
    border-radius: 3px;
}
QTableWidget#IpBatchTable {
    background: #ffffff;
    alternate-background-color: #f5f7fb;
    color: #17243a;
    border: 1px solid #d8e1ee;
    border-radius: 10px;
    gridline-color: #e7ecf3;
}
QTableWidget#IpBatchTable::item {
    padding-left: 8px;
    padding-right: 8px;
}
QTableWidget#IpBatchTable QHeaderView::section {
    background: #edf1ff;
    color: #4338ca;
    border: none;
    border-bottom: 1px solid #cdd7ef;
    padding: 8px 12px;
    font-weight: 700;
}


/* IP Batch numeric controls */
QSpinBox#ToolSpin {
    background: #f7f9fc;
    color: #17243a;
    border: 1px solid #d8e1ee;
    border-radius: 8px;
    padding: 6px 24px 6px 10px;
    selection-background-color: #6674f7;
}
QSpinBox#ToolSpin::up-button,
QSpinBox#ToolSpin::down-button {
    width: 18px;
    background: #edf1f7;
    border: none;
}


/* Signal Monitor structured controls */
QFrame#SignalControlsCard {
    background: #ffffff;
    border: 1px solid #d8e1ee;
    border-radius: 11px;
}
QLabel#SignalConnectionBadge {
    color: #526987;
    background: #e8eef9;
    border-radius: 12px;
    padding: 5px 11px;
    font-size: 11px;
    font-weight: 600;
}
