# ── Runtime dependencies ──────────────────────────────────────────────────────
# Зафиксированы минимальные проверенные версии.
# Для production-сборки используйте pip-compile requirements.in → requirements.txt
# с точными версиями всех транзитивных зависимостей.

# Core UI
PySide6>=6.7,<7.0

# System/typing
typing_extensions>=4.9

# Windows DPAPI (password encryption) — primary vault
pywin32>=306; sys_platform == "win32"

# System keyring (Windows Credential Manager) — fallback vault when pywin32 unavailable
keyring>=25.1

# Excel read/write (IP Batch Check tool)
openpyxl>=3.1.2

# Phase 1 (notifications - optional)
# winotify>=1.1
