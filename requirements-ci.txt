# Exact direct dependencies verified by the Ubuntu/Windows CI matrix.
# Transitive dependencies are resolved by pip for the selected platform.
PySide6==6.10.2
typing_extensions==4.15.0
pytest==9.0.3
ruff==0.15.10
